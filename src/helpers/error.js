// export function errorFormater(error){
// 	let message = 'you have ' + error.length + ' errors(';
// 	error.map((err, i) => {
// 		if(error.length === (i + 1)) {
// 			message += error[i].message + ').';
// 		}else{
// 			message += error[i].message + ', ';
// 		}
// 	});

// 	return message;
// }
